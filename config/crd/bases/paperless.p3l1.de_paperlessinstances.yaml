---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: paperlessinstances.paperless.p3l1.de
spec:
  group: paperless.p3l1.de
  names:
    kind: PaperlessInstance
    listKind: PaperlessInstanceList
    plural: paperlessinstances
    shortNames:
    - pli
    singular: paperlessinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PaperlessInstance is one Paperless-NGX installation.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            default: {}
            description: PaperlessInstanceSpec describes one Paperless-NGX installation.
            properties:
              admin:
                default: {}
                description: |-
                  AdminSpec controls the local superuser Paperless creates at startup. Set Enabled
                  to false for an installation where authentication is delegated entirely to an
                  OIDC provider and no local superuser account should exist.
                properties:
                  email:
                    description: Email address recorded on the superuser account.
                    type: string
                  enabled:
                    default: true
                    description: Enabled controls whether the operator ensures a local
                      superuser account exists.
                    type: boolean
                  passwordSecretRef:
                    description: PasswordSecretRef supplies the password; one is generated
                      when unset.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  username:
                    default: admin
                    description: Username of the local superuser account.
                    type: string
                type: object
              cache:
                default: {}
                description: |-
                  Cache selects between a managed Valkey instance and an external
                  Redis-compatible cache.
                properties:
                  managed:
                    default: true
                    description: |-
                      Managed selects whether the operator runs its own Valkey instance. Set this
                      to false and supply URL or URLSecretRef to use an external Redis-compatible cache.
                    type: boolean
                  url:
                    description: |-
                      URL is used when Managed is false and URLSecretRef is unset, e.g.
                      redis://cache:6379. Prefer URLSecretRef when the URL carries a password:
                      this field is stored as plain text.
                    type: string
                  urlSecretRef:
                    description: |-
                      URLSecretRef supplies the cache URL from a Secret key, used when Managed is
                      false. Preferred over URL whenever the connection string carries a credential.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
                x-kubernetes-validations:
                - message: set cache.url or cache.urlSecretRef when cache.managed
                    is false; leave both unset when the operator manages the cache
                  rule: '(!has(self.managed) || self.managed) ? (!has(self.url) &&
                    !has(self.urlSecretRef)) : ((has(self.url) && self.url != "")
                    || has(self.urlSecretRef))'
              database:
                description: |-
                  Database selects between a managed CloudNativePG cluster and an external
                  PostgreSQL server.
                properties:
                  cnpg:
                    description: CNPG configures the CloudNativePG cluster the operator
                      creates and manages.
                    properties:
                      instances:
                        default: 1
                        description: |-
                          Instances is the number of PostgreSQL instances CloudNativePG runs for
                          high availability.
                        format: int32
                        minimum: 1
                        type: integer
                      storage:
                        default:
                          size: 10Gi
                        description: Storage is the PersistentVolumeClaim template
                          CloudNativePG uses for each instance.
                        properties:
                          accessModes:
                            description: AccessModes the claim requests; the storage
                              class's default applies when unset.
                            items:
                              type: string
                            type: array
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Size is the storage capacity requested for
                              the claim.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: |-
                              StorageClassName is omitted from the claim when unset, letting the cluster
                              choose its default. An empty string disables dynamic provisioning.
                            type: string
                        type: object
                    type: object
                  external:
                    description: External points at a PostgreSQL database the operator
                      does not manage.
                    properties:
                      credentialsSecretRef:
                        description: CredentialsSecretRef must hold "username" and
                          "password" keys.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      host:
                        description: Host is the PostgreSQL server's DNS name or IP
                          address.
                        type: string
                      name:
                        default: paperless
                        description: |-
                          Name of the database to connect to inside the PostgreSQL server. This is a
                          database name, not a Kubernetes object name.
                        type: string
                      port:
                        default: 5432
                        description: Port the PostgreSQL server listens on.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    required:
                    - credentialsSecretRef
                    - host
                    type: object
                type: object
                x-kubernetes-validations:
                - message: set either database.cnpg or database.external, not both
                  rule: '!(has(self.cnpg) && has(self.external))'
              deletionPolicy:
                default: Retain
                description: |-
                  DeletionPolicy controls what becomes of your documents, database and
                  generated credentials when this PaperlessInstance is deleted. Retain
                  (the default) leaves them in place: `kubectl delete` on this instance
                  does not delete your documents, and creating a new instance with the
                  same name adopts them. Delete removes them together with the instance,
                  leaving nothing behind to reclaim but nothing left to recover either.
                enum:
                - Retain
                - Delete
                type: string
              env:
                description: Env passes any PAPERLESS_* variable the typed fields
                  do not cover.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: |-
                                The key to select from the ConfigMap's Data field.
                                Keys in the BinaryData field are not currently propagated to container env vars.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              envFrom:
                description: |-
                  EnvFrom loads environment variables in bulk from ConfigMaps or Secrets. Env
                  entries take precedence over an EnvFrom entry of the same name.
                items:
                  description: EnvFromSource represents the source of a set of ConfigMaps
                    or Secrets
                  properties:
                    configMapRef:
                      description: The ConfigMap to select from
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the ConfigMap must be defined
                          type: boolean
                      type: object
                      x-kubernetes-map-type: atomic
                    prefix:
                      description: |-
                        Optional text to prepend to the name of each environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    secretRef:
                      description: The Secret to select from
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the Secret must be defined
                          type: boolean
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              image:
                default: {}
                description: Image selects the Paperless-NGX container image.
                properties:
                  digest:
                    description: |-
                      Digest pins an exact image, in the form "sha256:<64 hex characters>", and
                      takes precedence over Tag.
                    pattern: ^sha256:[0-9a-f]{64}$
                    type: string
                  pullPolicy:
                    default: IfNotPresent
                    description: |-
                      PullPolicy controls when the kubelet re-pulls the image; the default only
                      pulls when it is not already present on the node.
                    type: string
                  pullSecrets:
                    description: PullSecrets authenticate image pulls from a private
                      registry.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  repository:
                    default: ghcr.io/paperless-ngx/paperless-ngx
                    description: Repository holds the image without tag or digest.
                    type: string
                  tag:
                    default: 3.1.3
                    description: Tag selects a released version. Ignored when Digest
                      is set.
                    type: string
                type: object
              ocr:
                default: {}
                description: OCRSpec configures text recognition.
                properties:
                  languages:
                    default:
                    - eng
                    description: |-
                      Languages are Tesseract language codes (e.g. "eng", "deu") to recognise text
                      in. Each one must already be installed in the image; an unavailable code
                      degrades OCR silently rather than failing.
                    items:
                      type: string
                    type: array
                type: object
              resources:
                description: Resources applied to the Paperless container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              secretKeySecretRef:
                description: SecretKeySecretRef supplies Django's secret key; one
                  is generated when unset.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              storage:
                default: {}
                description: Storage configures the four PersistentVolumeClaims Paperless
                  uses.
                properties:
                  consume:
                    default:
                      size: 5Gi
                    description: |-
                      Consume is the watched inbox: documents placed here are picked up for
                      ingestion and removed once processed. Needs a ReadWriteMany-capable storage
                      class if the Deployment ever runs more than one replica.
                    properties:
                      accessModes:
                        description: AccessModes the claim requests; the storage class's
                          default applies when unset.
                        items:
                          type: string
                        type: array
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the storage capacity requested for the
                          claim.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is omitted from the claim when unset, letting the cluster
                          choose its default. An empty string disables dynamic provisioning.
                        type: string
                    type: object
                  data:
                    default:
                      size: 5Gi
                    description: |-
                      Data holds Paperless's own working state: the search index, the
                      classification model, and SQLite artefacts if used. Needs a
                      ReadWriteMany-capable storage class if the Deployment ever runs more than
                      one replica.
                    properties:
                      accessModes:
                        description: AccessModes the claim requests; the storage class's
                          default applies when unset.
                        items:
                          type: string
                        type: array
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the storage capacity requested for the
                          claim.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is omitted from the claim when unset, letting the cluster
                          choose its default. An empty string disables dynamic provisioning.
                        type: string
                    type: object
                  export:
                    default:
                      size: 10Gi
                    description: |-
                      Export is the destination for document_exporter runs, and doubles as where a
                      backup lands. Needs a ReadWriteMany-capable storage class if the Deployment
                      ever runs more than one replica.
                    properties:
                      accessModes:
                        description: AccessModes the claim requests; the storage class's
                          default applies when unset.
                        items:
                          type: string
                        type: array
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the storage capacity requested for the
                          claim.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is omitted from the claim when unset, letting the cluster
                          choose its default. An empty string disables dynamic provisioning.
                        type: string
                    type: object
                  media:
                    default:
                      size: 20Gi
                    description: |-
                      Media holds the document archive itself: originals, archived PDFs, and
                      thumbnails. It grows without bound and is the volume to size deliberately,
                      since not every storage class supports expanding a claim later. Needs a
                      ReadWriteMany-capable storage class if the Deployment ever runs more than
                      one replica.
                    properties:
                      accessModes:
                        description: AccessModes the claim requests; the storage class's
                          default applies when unset.
                        items:
                          type: string
                        type: array
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the storage capacity requested for the
                          claim.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is omitted from the claim when unset, letting the cluster
                          choose its default. An empty string disables dynamic provisioning.
                        type: string
                    type: object
                type: object
              timezone:
                default: UTC
                description: |-
                  Timezone is an IANA tz database name, e.g. "Europe/Berlin", that Paperless
                  uses to display and file dates.
                type: string
              url:
                description: |-
                  URL is the external address Paperless builds links and callbacks from, e.g.
                  https://paperless.example.org.
                pattern: ^https?://
                type: string
            type: object
          status:
            description: PaperlessInstanceStatus reports what the operator observes.
            properties:
              conditions:
                description: |-
                  Conditions are the operator's most recent observations of the instance's
                  state, keyed by type so Server-Side Apply merges entries instead of
                  replacing the whole list.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the metadata.generation the conditions above were
                  last computed from.
                format: int64
                type: integer
              url:
                description: URL echoes the address the instance answers on.
                type: string
              version:
                description: Version is what the running instance reports about itself.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
